#include "main.h"

/**
* add - a function that adds two integers
* @i: first number input
* @j: second number input
* Return: result
*/

int add(int i, int j)
{
	int result = (i + j);

	return (result);
}
