#include "main.h"
#include <stdio.h>
/**
 * _islower - check the code.
 *
 * Return: Always 0.
 *@c:character and parametre of the function
 */

int_islower(int c)
{
	if (c >= 97 && c <= 122)
	return (1);

	else
	return (0);
}
